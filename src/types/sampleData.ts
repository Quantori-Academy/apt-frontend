export type SampleData = {
  name: string;
  description: string;
  structure: string;
  price_per_unit: number;
  quantity_unit: string;
  quantity_left: number;
  expiration_date: string;
  location_id: number;
  added_substance_ids: number[];
};
