export type SampleData = {
  name: string;
  description: string;
  structure: string;
  pricePerUnit: number;
  quantityUnit: string;
  quantityLeft: number;
  expirationDate: string;
  locationId: number;
  addedSubstanceIds: number[];
};
