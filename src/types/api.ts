export type ApiStatus = {
  status: string;
  message: string;
};

export type Token = {
  token: string;
};

export type UserDetails = {
  userName: string;
  firstName: string;
  lastName: string;
  email: string;
};
